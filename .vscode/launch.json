{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "playdate",
            "request": "launch",
            "name": "Playdate: Build & Run",
            "preLaunchTask": "${defaultBuildTask}",
            "source": "${workspaceFolder}/Source",
            "output": "${workspaceFolder}/god_klaw.pdx"
        }
  ]
}
